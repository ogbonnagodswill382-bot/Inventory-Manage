services:
  - type: web
    name: stockflow-backend
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn stockflow_backend.wsgi:application"
    envVars:
      - key: DEBUG
        value: "False"
